# 설계 원칙 (공개 요약본)

이 하네스가 지키는 엔지니어링 원칙의 공개 요약입니다. 소유자 전용 세부(계정·경로·개인 절차)는
비공개 문서에 있으며, 여기엔 누구나 가져다 쓸 수 있는 일반 원칙만 담습니다.

1. **단일 소스.** 사실마다 정본 하나 — 코어 규칙은 `~/.agents/AGENTS.md`, 모델 id는
   `models.toml`. 나머지는 임포트/라우팅하고, 불가피한 미러는 드리프트 검사로 감시한다.
2. **AoE(필요 시 로드).** 상시 로드는 자격을 증명한 것만. 슬래시 옵트인, 온디맨드 가이드,
   라이브러리 설치 패턴. 새 자산의 기본 컨텍스트 비용 ≈ 메타데이터 한 줄 — 트랜스크립트
   실측으로 증명한다.
3. **증거 우선.** 읽고 측정한 뒤 말한다. 하드테스트는 자기보고가 아니라 실제 헤드리스 세션과
   트랜스크립트 grep으로 한다. 외부 사실은 신선한 웹 확인을 거친다.
4. **벤더본은 프리스틴.** 업스트림 커밋을 핀하고 본문은 절대 수정하지 않는다. 커스텀은
   설치 시점 변환이나 우리 쪽 오버레이 파일에서만. 갱신 경로(update-vendored.sh)를 남긴다.
5. **공개/비공개 강분리.** 개인 상태(메모리·세션·크리덴셜·사용자 컨텍스트)는 배포판에 절대
   들어가지 않는다 — 사람의 주의가 아니라 테스트(새니타이즈 grep + 계약)가 지킨다.
6. **최소 diff, YAGNI** — 단, 이해를 건너뛰는 게으름은 금지. 흐름을 끝까지 읽고 나서
   가장 작은 동작하는 변경을 고른다.
7. **정책 변경은 테스트와 함께 착지한다.** 의도된 변경은 같은 세션에서 계약(셀프테스트)을
   리핀한다. 그린(0 fail)이 완료의 정의다.
8. **배포 규율.** 공개 배포판은 단일 `init` 커밋. 파괴적 작업(force push)은 단일 사용
   승인 절차로만, 영구 삭제는 사용자 손으로만.
9. **크로스플랫폼 1급.** macOS와 Windows 둘 다 출하하고 둘 다 검증한다(페이크 HOME 리허설,
   PowerShell 파서 검증, 임시경로 분기).
10. **기본값은 가역.** 삭제 대신 백업, 드롭 대신 스태시, 유일 히스토리는 아카이브. 위험한
    장치엔 fail-open과 비상 스위치를 함께 단다.
