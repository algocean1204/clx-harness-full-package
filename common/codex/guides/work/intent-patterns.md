# Intent Failure Patterns — Self-Growing Ledger

Shared store for both platforms (Codex reads/writes via symlink). Consulted during the alignment-loop JUDGE step: `rg -i '<task domain keywords>' ~/.claude/guides/work/intent-patterns.md` — past failures are pre-loaded traps to avoid.

Append protocol (triggered by `intent-lock.py` correction capture on Claude; by `work/intent` §5 on Codex): one table row per pattern — judge first (real fidelity failure vs mere preference), rg for duplicates, merge similar rows instead of adding near-duplicates. Keep rules imperative and testable.

This ledger ships empty. Rows accumulate from your own corrections as you use the harness — it is a personal learning store, so nothing is pre-seeded.

| Date | Context | Failure pattern | Rule |
|------|---------|-----------------|------|
