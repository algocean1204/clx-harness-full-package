# Working preferences

TEMPLATE-UNFILLED — delete this line once you have filled the file in.

> Facts only — policy source of truth is `~/.agents/AGENTS.md` core rules; on divergence the
> core wins. Write preferences here, not rules.

- Autonomy: how much should run without asking, and where the stop points are:
- Reports: language, form, length:
- Verification: what counts as proof before something is called done:
- Pet peeves (things that waste your time when the agent does them):
