---
description: Visualize how a request actually flows through this codebase — branches, joins, loops — in chat
argument-hint: <path, or a specific path/branch to draw deeply (default: whole project)>
---

Invoke skill `clx-flow-map` and follow it on:

$ARGUMENTS

Chat output is ASCII plus the branch table; mermaid only if the user names a file or artifact to write into. With no argument, map the whole project from its entry point.
